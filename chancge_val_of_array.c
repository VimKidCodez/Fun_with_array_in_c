#include <stdio.h>

int main()
{   // instalize an array
    int array[5] = {1,2,3,4,5};
    //change  the value of the first array to 0
    array[1] = 0 ;

    // Change the 5th array to 6
    array[5] = 6 ;
    

    return 0;
}